use typemap::TypeMap;



#[cfg(test)]
mod tests {
    #[test]
    //#[should_panic]
    fn tesssssss() {
        let mut map = TypeMap::new();
        map.
        assert!(true);
        assert_eq!(1+1, 2+0);
    }
}
